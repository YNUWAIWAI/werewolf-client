import {Language} from '../../src/i18n/landing'

const label = 'Language'
const options = {
  en: Language.en,
  ja: Language.ja
}
const defaultValue = options.en

export default {
  defaultValue,
  label,
  options
}
