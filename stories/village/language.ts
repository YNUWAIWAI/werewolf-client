/* global village */
const label = 'Language'
const options = {
  en: village.Language.en,
  ja: village.Language.ja
}
const defaultValue = options.en

export default {
  defaultValue,
  label,
  options
}
