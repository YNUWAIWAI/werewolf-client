import * as ActionTypes from '../constants/ActionTypes'

const initialState = {
  postCount: 0,
  postCountLimit: 10
}

const commandInput = (state = initialState, action) => state

export default commandInput

