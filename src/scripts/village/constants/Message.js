import {VERSION} from './Version'
const BASE_URL = `https://werewolf.world/resource/${VERSION}`

export const BOARD_MESSAGE = `${BASE_URL}/boardMessage`
export const ERROR_MESSAGE = `${BASE_URL}/errorMessage`
export const PLAYER_MESSAGE = `${BASE_URL}/playerMessage`
export const SCROLL_MESSAGE = `${BASE_URL}/scrollMessage`
export const SYSTEM_MESSAGE = `${BASE_URL}/systemMessage`
export const VOTE_MESSAGE = `${BASE_URL}/voteMessage`
