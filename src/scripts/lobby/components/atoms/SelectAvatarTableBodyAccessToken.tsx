import * as React from 'react'

interface Props {}

export default function SelectAvatarTableBodyAccessToken(props: Props) {
  return (
    <div className="lo--select-avatar--table--body--access-token">
      access token
    </div>
  )
}
